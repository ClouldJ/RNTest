import * as React from '@types/react'
import { inject, observer } from 'mobx-react'
import { Text, View } from 'react-native'
import Content from './Content'

export const FunctionPage = observer(() => {
  return <Content />
})
